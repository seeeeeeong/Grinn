<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.spring.report.dao.ReportMapper">
	<!-- 스타일, 댓글 신고 목록 -->
	<sql id="reportSearch">
		<where>
			<if test="keyword != null and keyword != ''">
				<if test="keyfield == 1"><!-- 상품번호 -->
					report_mem = #{rst_num}
				</if>
				<if test="keyfield == 2"><!-- 댓글번호 -->
					com_num = #{rcom_num}
				</if>				
			</if>
		</where>
	</sql>
	
	<!-- 전체/검색 레코드수 -->
	<select id="selectRowCount" parameterType="map" resultType="integer">
		SELECT COUNT(*) FROM report_st 
		LEFT OUTER JOIN member m ON s.mem_num = m.mem_num 
		LEFT OUTER JOIN member_detail d ON m.mem_num = d.mem_num
		<include refid="reportSearch"></include>
	</select>

	
	<!-- 전체/검색 목록 -->
	<select id="selectList" parameterType="map" resultType="styleVO">
		SELECT * FROM (SELECT a.*, rownum rnum FROM
		(SELECT st_num,
		<![CDATA[
		REPLACE(REPLACE(st_phrase, '<', '&lt;'), '>', '&gt;') st_phrase,
		]]>
		s.st_photo1n, s.st_photo2n, s.st_photo3n, s.mem_num, s.st_regdate, 
		s.st_mdate, s.item_num1, s.item_num2, s.item_num3, m.mem_id, d.mem_photo
		FROM style s LEFT OUTER JOIN member m ON s.mem_num = m.mem_num 
		LEFT OUTER JOIN member_detail d ON m.mem_num = d.mem_num ORDER BY st_num DESC)a)
		<![CDATA[
		WHERE rnum >= #{start} AND rnum <= #{end}
		]]>
	</select>
	
</mapper>